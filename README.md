# Basic-4-channel-CZI-Image-Processing

Run this on all czi images regardless of magnification. Run before any other analysis packages.

The initial input should be a folder of subfolders where each subfolder is the treatment/sample name as separated into 2 parts. E.g. "Tri_1ugLPS". Images within folders will be automatically renamed to the subfolder name + i. 
